# Journal des décisions

| Date | Décision | Motif | Impact |
| 2026-04-18 | Le lot 1 de refonte frontend pose des fondations UI transverses (tokens, shell, navigation, formulaires, feedback, tableaux, tabs) sans changement métier ni permissionnel | Préparer une refonte premium cohérente écran par écran sans déplacer la logique backend | Les lots suivants peuvent refondre landing, tunnel prospect, checkout et portails sur une base visuelle commune et testable |
|---|---|---|---|
| 2026-04-01 | UI initiale conservée dans Django avec templates + HTMX + Alpine + Tailwind | Réduire le coût de fondation tout en gardant une UX moderne et dynamique | Le Sprint 00 reste monorepo et ne crée pas de front séparé |
| 2026-04-01 | Settings séparés `base` / `dev` / `test` / `prod` avec configuration sensible uniquement via variables d’environnement | Poser une base sûre, testable et portable entre local, CI et conteneurs | Aucun secret hardcodé, CI et Docker utilisent la même structure de configuration |
| 2026-04-01 | User personnalisé dès le Sprint 00 avec `public_id` UUID | Éviter une migration auth coûteuse plus tard et préparer les ressources exposées non prédictibles | Les futures permissions et espaces client/staff s’appuieront sur `accounts.User` |
| 2026-04-01 | App `auditlog` et service minimal créés dès les fondations | Préparer la traçabilité des actions sensibles sans attendre les lots métier | Les futures features devront brancher explicitement l’audit plutôt que l’inventer localement |
| 2026-04-01 | Stack exécutable locale normalisée autour de Docker Compose + PostgreSQL + Redis + Celery + Gunicorn + Nginx | Garantir un socle reproductible et cohérent avec la cible de production | Le Sprint 00 est vérifiable par healthchecks, migrations, worker et proxy |
| 2026-04-01 | Authentification figée sur email | Le projet est B2B/pro et l’email est l’identifiant le plus stable et le moins ambigu | Sprint 01 peut construire comptes et rôles sans dette auth liée au `username` |
| 2026-04-01 | `Customer` devient le compte client minimal et `CustomerMembership` prépare le multi-utilisateur | Éviter de figer un modèle mono-contact avant les permissions objet | Le futur scope client/staff et les memberships sont prêts sans implémenter encore le RBAC |
| 2026-04-01 | Les statiques Docker sont servis par Nginx via volume dédié et les artefacts runtime sortent du source tree | Corriger le backoffice Docker et éviter les artefacts générés dans le repo | Admin exploitable dans la stack et runtime plus propre |
| 2026-04-01 | La CI ajoute une exécution sur PostgreSQL réel | Réduire l’écart entre les tests automatiques et le runtime cible | Les migrations et tests minimaux sont validés sur le moteur SQL principal |
| 2026-04-01 | `Customer` est confirmé comme frontière de tenant et racine d’isolation des objets client | Éviter toute dérive future vers des objets métier rattachés directement à `User` | Les futurs domaines client devront tous être filtrés par scope `Customer` |
| 2026-04-01 | Les rôles client restent localisés sur `CustomerMembership` avec le minimum `owner` / `member` | Préparer les permissions objet sans figer un RBAC trop tôt | Les actions client sensibles peuvent distinguer `owner` et `member` sans mélanger les rôles staff |
| 2026-04-01 | L’accès staff exige `is_staff` et la permission Django `accounts.access_staff_portal` | Empêcher qu’un simple compte staff technique ait un accès applicatif implicite | Les routes backoffice Sprint 01 refusent les clients standards et les staff non autorisés |
| 2026-04-01 | La préparation MFA staff est limitée à des flags de posture `staff_mfa_required` / `staff_mfa_enabled` | Préparer le durcissement backoffice sans bloquer le Sprint 01 sur une MFA complète | Le backoffice peut exposer l’état MFA sans engager encore le fournisseur ni les flows complets |
| 2026-04-01 | `/admin/` reste la surface d’administration technique, distincte du futur portail staff applicatif | Éviter de confondre admin Django et backoffice métier | Les accès staff applicatifs ne reposent pas sur `/admin/` |
| 2026-04-01 | Les changements sensibles déjà possibles sont tracés par un audit minimal | Réduire le risque d’élévation de privilège non visible | Les bascules de rôle, membership et posture staff laissent une trace exploitable |
| 2026-04-01 | Le compte hybride client/staff est figé par un test explicite de séparation de contexte | Prévenir une ambiguïté de scope lors des lots suivants | Les futures routes métier peuvent s’appuyer sur une séparation de contexte déjà testée |
| 2026-04-01 | Le lot catalogue / pricing / commande démarre avec un pricing unitaire simple et une commande minimale rattachée à `Customer` | Livrer un premier slice métier sans figer un moteur tarifaire prématuré | Les prochains lots pourront enrichir le pricing et le checkout sans casser la structure de base |
| 2026-04-01 | Les routes catalogue et commande doivent rester scoping server-side par `Customer` dès le premier lot métier | Réduire le risque de fuite horizontale avant l’ajout d’uploads et de workflow | Les tests d’accès croisé deviennent un prérequis du lot métier |
| 2026-04-01 | Le pricing bootstrap Sprint 03 est porté par `CatalogService.base_price` et encapsulé par `PricingService` | Garder le modèle simple tout en préservant un point d’extension clair | Les futures grilles revendeur et règles avancées pourront enrichir le service sans casser les routes déjà exposées |
| 2026-04-01 | Les commandes snapshotent les données catalogue utiles (`service_code`, `service_name`, `service_type`, `unit`, prix) à la création | Préserver la cohérence métier si le catalogue évolue ensuite | Les prochains lots uploads/paiement/workflow s’appuieront sur une commande stable et traçable |
| 2026-04-01 | `OrderService.create_order` valide désormais explicitement le scope acteur / `Customer` et peut accepter un `customer_membership` déjà validé | Éviter une future réutilisation dangereuse du service d’écriture hors de la vue client actuelle | Les prochains appels au service doivent fournir un acteur autorisé ou un scope cohérent, sinon ils échouent côté backend |
| 2026-04-01 | Les endpoints staff catalogue et commandes exigent `access_staff_portal` plus une permission métier de lecture par domaine | Sortir du modèle staff trop large avant les lots sensibles | La surface staff du slice reste minimale et prépare un RBAC plus fin sans refacto large |
| 2026-04-01 | Un test API explicite couvre l’injection malveillante de `unit_price`, `line_total` et `total_amount` | Prouver que le backend recalcule toujours les prix côté serveur | Les régressions futures sur le pricing exposé par API seront détectées automatiquement |
| 2026-04-02 | Les uploads de commande vivent dans une app Django dédiée `uploads` | Conserver une frontière de domaine claire sans alourdir `orders` | Le modèle et les services fichiers restent isolés du cœur commande |
| 2026-04-02 | Tout accès fichier de commande est médié par backend et scoped via `Order -> Customer` | Empêcher l’exposition publique brute et les fuites horizontales entre clients | Les routes client/staff fichiers exigent un contrôle serveur explicite avant tout téléchargement |
| 2026-04-02 | La lecture staff des fichiers exige une permission dédiée distincte de la lecture commande | Éviter qu’une permission opérationnelle trop large réouvre l’accès aux fichiers | La surface staff fichiers reste séparée des routes staff commandes |
| 2026-04-02 | Le contrôle fichier basique est porté par `OrderUploadInspection` en `OneToOne` avec `OrderUpload` | Ajouter une fondation extensible pour les futurs contrôles sans surcharger `OrderUpload` ni réintroduire un scope direct sur `Customer` | Les prochains contrôles pourront enrichir ou compléter le rapport sans casser les routes d’upload existantes |
| 2026-04-02 | L’extraction de métadonnées Sprint 04 reste limitée à un socle synchrone et léger (PDF minimal, PNG/JPEG par lecture d’en-tête) | Livrer une première vérification sûre sans dépendance externe ni traitement long | Les types non couverts reçoivent un `warning` documenté et le futur contrôle DTF avancé pourra passer en service plus riche ou asynchrone |
| 2026-04-02 | La consultation staff du contrôle fichier exige une permission dédiée `uploads.view_orderuploadinspection` | Séparer la lecture du rapport de contrôle de la simple lecture du fichier brut | Le backoffice garde un principe de moindre privilège sur la surface contrôle |
| 2026-04-02 | Le Sprint 04 se concentre sur un contrôle fichier basique rattaché à `OrderUpload` plutôt que sur une analyse avancée | Préparer la montée en puissance vers le contrôle DTF sans sur-ingénierie | Le lot peut exposer un statut simple, des métadonnées de base et un détail consultable sans figer la V2 |
| 2026-04-02 | Les métadonnées et résultats de contrôle restent strictement scoped et ne divulguent ni chemin disque ni URL brute | Réduire le risque de fuite de stockage tout en gardant un support client/staff utile | Les sérialisations futures doivent rester médiées par backend et ne publier que des données sûres |
| 2026-04-02 | Les erreurs de fichier lisible, illisible ou inattendu sont normalisées côté serveur dans un statut simple | Éviter des messages divergents entre routes client et staff | Les vues consomment un résultat de contrôle homogène, plus facile à auditer et à tester |
| 2026-04-02 | Le lot Google Drive s'appuie sur un Shared Drive unique avec mapping DB des IDs et backend source de vérité | Garantir une synchronisation traçable, idempotente et sans exposition de lien brut client | Les services Drive doivent encapsuler l'API, conserver les statuts en base et journaliser les événements utiles |
| 2026-04-02 | Le Sprint 05 persiste séparément l'arborescence Drive de commande (`OrderDriveFolder`) et l'état de sync par upload (`OrderUploadDriveSync`) | Éviter de surcharger `OrderUpload` tout en gardant un modèle minimal, auditables et réutilisable pour la suite atelier | Les futurs lots production pourront réutiliser le dossier commande existant sans casser le scope uploads |
| 2026-04-02 | La consultation staff de la sync Drive exige la permission dédiée `uploads.view_orderuploaddrivesync` | Appliquer le moindre privilège et éviter que la simple lecture de fichiers réouvre trop largement la surface d'intégration Drive | Le backoffice expose l'état de sync séparément sans publier d'ID ni de lien Drive dans l'API |
| 2026-04-02 | Le workflow production vit dans une app dédiée `production` avec un `OneToOne` vers `Order` | Isoler le domaine atelier sans surcharger `orders` et garder une frontière claire pour scan/logistique plus tard | Les futures features scan, OF enrichi et expédition réutiliseront le même agrégat métier |
| 2026-04-02 | Les routes staff production exigent `accounts.access_staff_portal` plus des permissions métier dédiées de lecture et de transition | Éviter une permission staff fourre-tout sur un domaine sensible | La surface atelier reste staff-only, auditable et conforme au moindre privilège |
| 2026-04-02 | La machine à états atelier Sprint 06 est limitée à `queued` / `in_progress` / `ready_to_ship` / `blocked` / `completed` | Livrer une base minimale compatible avec la future logistique et le futur scan sans figer un workflow trop riche trop tôt | Les lots scan et expédition pourront enrichir les transitions sans casser la fondation livrée |
| 2026-04-02 | Le Sprint 07 standardise un identifiant scannable opaque sur `ProductionJob` et impose des routes staff-only pour résolution et transition via scan | Préserver l’isolation client et garder le scan comme simple entrée de workflow, jamais comme surface métier autonome | Les prochains écrans atelier pourront s’appuyer sur un payload JSON sûr et un service de résolution unique |
| 2026-04-02 | Le Sprint 08 introduit une expédition Sendcloud minimale via une app `shipping`, un modèle `Shipment` lié à `Order`, des routes staff-only et un service d’intégration centralisé | Livrer une base logistique sûre sans surconstruire le cycle complet d’expédition | Le tracking, le polling, le portail client et les enrichissements futurs pourront évoluer sans casser le socle MVP |
| 2026-04-02 | Le MVP shipping reste limité à une expédition par commande et exige l’état atelier `ready_to_ship` avant création | Garder la cohérence métier avec le workflow atelier tout en restant minimal | Le socle d’expédition refuse les créations prématurées et les doublons sur une même commande |
| 2026-04-02 | La lecture et la création staff d’expédition exigent `shipping.view_shipment` et `shipping.create_shipment` en plus de `accounts.access_staff_portal` | Appliquer le moindre privilège sur un domaine sensible et intégré à un tiers | La surface shipping reste séparée des lectures commandes génériques et préparée pour un RBAC plus fin |
| 2026-04-02 | Le Sprint 09 UI produit est implemente via une app `portal` Django templates + HTMX avec surfaces client/staff separees | Livrer vite une UI moderne sans dupliquer les regles metier ni creer de vue backend monolithique | Les panneaux commande chargent des fragments scopees et reutilisent les services existants, avec permissions backend conservees |
| 2026-04-02 | Le portail adopte une page de login dediee `/login/` avec redirection post-login par scope (staff/client) | Reduire la confusion entre admin technique et portail applicatif, et fiabiliser la recette UX pre-paiement | Les routes portail n'utilisent plus `/admin/login/` comme point d'entree par defaut tout en conservant `/admin/` pour les superusers |
| 2026-04-02 | Le Sprint 10 introduit une app `billing` dediee avec `Payment` et `Invoice` relies a `Order` | Centraliser la logique paiement/facturation et eviter de disperser des regles sensibles dans les vues | Les routes client/staff/backend consomment un service central et conservent le scope `Order -> Customer` |
| 2026-04-02 | La confirmation PayPal MVP passe par une route backend interne tokenisee et idempotente | Eviter une capture non autorisee et reduire le risque de double traitement | Les captures sont rejouables sans doublon de facture et les audits tracent succes/echecs |
| 2026-04-02 | La facture MVP est generee automatiquement cote serveur au format texte medie apres paiement `captured` | Livrer rapidement une preuve de facturation exploitable sans introduire une stack PDF complexe | Le client peut lire/telecharger sa facture scopee, et le staff autorise dispose d'une vue dediee |
| 2026-04-02 | Mini-lot Sprint 10.1 : les tests `shipping` obsoletes sont realignes sur le contrat de `ShipmentService` sans modification metier | Fermer la reserve QA sans introduire de changement fonctionnel hors perimetre | Le contrat service `shipping` reste stable ; la suite de tests couvre le schema payload et les champs actuellement exposes |
| 2026-04-02 | Le Sprint 11 UX conserve l'architecture portail Django + HTMX et ajoute des panneaux facture/billing scopes | Ameliorer le tunnel produit final sans ouvrir un nouveau domaine ni deplacer la logique metier vers l'UI | Les surfaces client/staff gagnent en lisibilite et feedback, avec permissions backend intactes et tests portail renforces |
| 2026-04-02 | Le micro-sprint 11.1 bis ajoute une couche front-office premium (`/`, `/services/`) et un tunnel client moderne scope (`/client/.../checkout/`) | Augmenter la perception premium et la conversion sans ouvrir un nouveau domaine ni casser la securite backend | Le frontend client devient plus e-commerce, mais toute creation commande/upload/paiement reste centralisee dans les services backend existants |
| 2026-04-02 | Le micro-sprint 11.2 transforme `/` en landing premium conversion-first via sections modulaires (hero, reassurance, differenciation, services, process, preuve, CTA) | Clarifier la proposition de valeur en quelques secondes et renforcer la confiance avant connexion | Amelioration UI/UX majeure sans impact metier ni changement de permissions backend |
| 2026-04-18 | Le hardening UI transverse renforce contraste, hiérarchie visuelle, badges, onglets workflow et feedback via composants partagés / CSS commun | Corriger l’effet délavé et homogénéiser la perception premium sans toucher au métier | Landing, prospect, checkout, portail client et staff gagnent en lisibilité tout en gardant la logique backend inchangée |
| 2026-04-18 | Le lot landing + prospect renforce rythme marketing, CTA, shell tunnel, stepper et lisibilité formulaire via templates/CSS partagés | Corriger le manque de contraste et de hiérarchie sur les surfaces d’acquisition et d’onboarding | La vitrine gagne en crédibilité conversion et le tunnel prospect devient plus guidé, sans impact métier ni permissionnel |
| 2026-07-11 | Les projets B2B vivent dans l'app dédiée `b2b_order_projects` avant `Order` | Ne déclencher ni production ni pricing définitif pendant la préparation client | La conversion future sera le seul point d'entrée vers le workflow de commande existant |
| 2026-07-11 | L'accès projet exige un flag global et `Customer.b2b_order_projects_enabled` | Matérialiser l'approbation IDS sans plateforme de flags ni RBAC parallèle | Le déploiement peut être progressif client par client et désactivé globalement |
| 2026-07-12 | `Nouvelle commande` remplace l'ancien upload pour tous les clients actifs lorsque le flag global est actif ; le flag client est déprécié | Garantir un parcours client unique et éviter qu'un compte conserve silencieusement l'ancien checkout | Le flag global reste le coupe-circuit ; memberships, scopes `Customer` et UUID publics restent obligatoires |
| 2026-07-12 | La transmission exige une confirmation client liée à la version analysée courante de chaque visuel | Rendre visibles les dimensions, la qualité de résolution et les alertes avant engagement de production | Toute modification de dimensions ou version de fichier invalide la confirmation ; l'action est auditée et tenant-scoped |
| 2026-07-12 | Les détails imprimés strictement inférieurs à 0,5 mm sont détectés pendant l'analyse asynchrone et matérialisés par un overlay privé | Alerter le client sur les éléments DTF fragiles sans altérer le fichier source ni bloquer arbitrairement la validation | La modale affiche les zones en rouge avec un contrôle afficher/masquer ; l'overlay reste versionné, tenant-scoped et présenté comme indicatif |
| 2026-07-12 | La présence de détails sous 0,5 mm impose une couleur support unie et explicite avant confirmation | L'atelier doit ajouter un contour assorti pour préserver les détails fins, et une mauvaise couleur peut rendre ce contour visible après pressage | La règle est contrôlée dans le service métier, le choix multicolore est refusé et la modale fournit un zoom 100–400 % pour inspecter les zones concernées |
| 2026-07-11 | Le rattachement fichier est différé jusqu'à un ADR `Asset` partagé | `OrderUpload` exige déjà une commande et un upload projet parallèle créerait une dette | Le Sprint 2 doit introduire une migration additive, versionnée et sans déplacement initial des fichiers |
| 2026-07-11 | L'ADR Asset partagé est accepté : `Asset`, versions immuables et analyse vivent dans `uploads` | Réutiliser validation et stockage sans rendre `OrderUpload.order` nullable ni dupliquer un `ProjectUpload` | Les projets B2B disposent de fichiers avant commande ; le backfill historique ne déplace aucun fichier et reste non destructif |
